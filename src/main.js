import './assets/style.css'

import { createApp } from 'vue'
import App from './App.vue'

//Creates Vue application instance using the App component and mounts it to the element with the id="app" --> index.html <div id="app">
createApp(App).mount('#app')

